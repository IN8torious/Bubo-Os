set timeout=3
set default=0

menuentry "Raven OS — Kernel Pluto: Red Cloud" {
    multiboot2 /boot/raven.kernel
    boot
}

menuentry "Raven OS — Safe Mode (nomodeset)" {
    multiboot2 /boot/raven.kernel nomodeset
    boot
}
